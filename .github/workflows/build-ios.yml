name: Build iOS IPA

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Expo prebuild (generate native iOS project)
        run: npx expo prebuild --platform ios --clean

      - name: List iOS directory (debug)
        run: ls -la ios/

      - name: Find workspace name
        id: workspace
        run: |
          WORKSPACE=$(find ios -maxdepth 1 -name "*.xcworkspace" -not -name "contentsdata*" | head -1 | xargs basename | sed 's/.xcworkspace//')
          if [ -z "$WORKSPACE" ]; then
            echo "No workspace found, listing ios dir:"
            ls -la ios/
            find ios -name "*.xcworkspace"
            exit 1
          fi
          echo "name=$WORKSPACE" >> $GITHUB_OUTPUT
          echo "Found workspace: $WORKSPACE"

      - name: Install CocoaPods
        run: |
          cd ios
          pod install

      - name: Build app (unsigned)
        run: |
          cd ios
          SCHEME=$(xcodebuild -workspace "${{ steps.workspace.outputs.name }}.xcworkspace" -list | grep -A1 "Schemes:" | tail -1 | xargs)
          echo "Using scheme: $SCHEME"
          xcodebuild \
            -workspace "${{ steps.workspace.outputs.name }}.xcworkspace" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath ${{ github.workspace }}/build/derived \
            -destination "generic/platform=iOS" \
            build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            COMPILER_INDEX_STORE_ENABLE=NO

      - name: Create IPA
        run: |
          echo "=== Looking for .app ==="
          find build/derived/Build/Products -name "*.app" -type d 2>/dev/null || true
          find build/derived -name "*.app" -type d 2>/dev/null || true
          APP_PATH=$(find build/derived/Build/Products -name "*.app" -type d 2>/dev/null | head -1)
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find build/derived -name "*.app" -type d 2>/dev/null | head -1)
          fi
          if [ -z "$APP_PATH" ]; then
            echo "ERROR: No .app found. Listing Products dir:"
            find build/derived/Build -type d | head -20
            exit 1
          fi
          echo "Found app at: $APP_PATH"
          mkdir -p build/Payload
          cp -r "$APP_PATH" build/Payload/
          cd build
          zip -r MellyPlus.ipa Payload
          rm -rf Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Melly-Plus-IPA
          path: build/MellyPlus.ipa
          retention-days: 30
